apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
  - name: TimescaleDB
    type: postgres
    access: proxy
    url: timescaledb:5432
    database: trading
    user: trading
    secureJsonData:
      password: trading_dev
    jsonData:
      sslmode: disable
